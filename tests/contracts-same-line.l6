//test return 100

int sum(int n)
//@requires n >= 0; requires n >= 0; requires n >= 0; requires n >= 0;
{
    int result = 0;
    for (int i = 0; i < n; i++)
    {
        result++;
    }
    return result;
}

int main() {
    return sum(100);
}