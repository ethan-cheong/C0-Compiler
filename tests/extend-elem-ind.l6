//test return 0

// Can call both index and elem together

int main() {
  int[] A = alloc_array(int, 5);
  
  for (int i = 0; i < 5; i++) {
    A[i] = i * i;
  }

  //@assert \forall {A} (\element == \index * \index);
  return 0;
}