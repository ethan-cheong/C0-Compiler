//test return 101

int sum(int n)
//@requires n >= 0;
//@requires n >= 0;
//@requires n >= 0;
//@requires n >= 0;
/*@ensures \result >= n;
ensures \result >= n;
ensures \result >= n;
ensures \result >= n;
ensures \result >= n;
ensures \result >= n;
@*/
{
    int result = 0;
    for (int i = 0; i < n+1; i++)
    {
        //@loop_invariant i < n + 2;
        //@loop_invariant i < n + 2;
        //@loop_invariant i < n + 2;
        //@loop_invariant i < n + 2;
        result++;
    }
    return result;
}

int main() {
    return sum(100);
}