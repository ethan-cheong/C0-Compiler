//test error

// Not allowed to use exists or forall when not yet declared

int main() 
//@loop_invariant \exists {arr} (\element == 0);
  {
  int[] arr= alloc_array(int, 100);

  for (int i=0; i<99; i++) {
    arr[i] = 100;
  }

  return 0;
}